syntax = "proto3";

package com.mycompany.smartfridge;

service TempService{
    rpc temp(TempRequest) returns (TempReply);
    rpc increasetemp(TempRequest) returns (TempReply);
}

message TempRequest {
    int32 temp = 1;
}

message TempReply {
    string output = 1;
}