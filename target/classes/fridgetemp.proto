syntax = "proto3";

package com.mycompany.smartfridge;

service TempService{
    rpc temp(TempRequest) returns (TempReply);
}

message TempRequest {
    string name = 1;
}

message TempReply {
    string greeting = 1;
}